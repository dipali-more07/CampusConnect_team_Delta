# ============================================================
# CampusConnect - Python Dependencies
# ============================================================
# Run: pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi>=0.111.0          # Our main web framework - handles HTTP requests
uvicorn[standard]>=0.30.1  # ASGI server - runs our FastAPI app
gunicorn>=22.0.0           # WSGI HTTP Server for production deployment


# --- Database ---
sqlalchemy>=2.0.30         # ORM - lets us write Python instead of raw SQL
psycopg2-binary>=2.9.9     # PostgreSQL adapter - connects Python to PostgreSQL
alembic>=1.13.1            # Database migrations - tracks DB schema changes

# --- Authentication & Security ---
python-jose[cryptography]>=3.3.0  # JWT token creation and verification
passlib[bcrypt]>=1.7.4            # Password hashing (bcrypt is the gold standard)
bcrypt>=4.0.0                      # bcrypt algorithm used by passlib

# --- Validation ---
pydantic>=2.7.1            # Data validation - checks request/response shapes
pydantic-settings>=2.3.0   # Settings management from .env files
email-validator>=2.1.0     # Validates email addresses

# --- File Handling ---
python-multipart>=0.0.9    # Handles file uploads in FastAPI
Pillow>=10.4.0             # Image processing (resize profile pictures, etc.)
aiofiles>=23.2.1           # Async file reading/writing

# --- QR Code ---
qrcode[pil]>=7.4.2         # Generate QR codes for events/certificates

# --- PDF Generation ---
reportlab>=4.2.0           # Generate certificate PDFs

# --- Email ---
fastapi-mail>=1.4.1        # Send emails (verification, password reset)

# --- Rate Limiting ---
slowapi>=0.1.9             # Rate limiting to prevent API abuse

# --- CORS & Middleware ---
python-dotenv>=1.0.1       # Load environment variables from .env file

# --- Testing ---
pytest>=8.2.2              # Test framework
pytest-asyncio>=0.23.7     # Async test support
httpx>=0.27.0              # HTTP client used by FastAPI test client
faker>=25.2.0              # Generate fake data for tests

# --- Utilities ---
python-slugify>=8.0.0      # Convert text to URL-friendly slugs
pytz>=2024.1               # Timezone handling
